# Optional: automatic email delivery for the signup form (https://web3forms.com).
# Create a form, set the notification inbox in the Web3Forms dashboard, then paste your access key.
# If unset, submit opens the visitor’s email app with a pre-filled message (mailto fallback).
VITE_WEB3FORMS_ACCESS_KEY=
